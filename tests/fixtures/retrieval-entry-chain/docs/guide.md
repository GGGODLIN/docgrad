# Guide

## Rules

- **MUST**: This rule carries a coordinate, `src/foo/bar.ts`, and is deliberately verbose so the
  token estimate is large enough for a double-counted file to show up as an obvious difference.
