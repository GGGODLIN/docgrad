Run a full docgrad audit (`/docgrad audit`) on the repo at `evals/fixtures/planted-contradiction/`
under the docgrad plugin directory, treating that fixture's path as the target repo root.

Output the complete scorecard, along with the correctness dimension's claim-ledger table and
cumulative coverage.
